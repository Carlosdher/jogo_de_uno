from django.contrib import admin
from .models import Cartas, User, Partida

# Register your models here.
admin.site.register(Cartas)
admin.site.register(User)
admin.site.register(Partida)

# Register your models here.
